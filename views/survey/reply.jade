extends ../layout

block content
  .container
    h1.page-header 설문조사 응답
    h3.page-header= survey.title
    h3 by #{survey.email}
    h3 설문 설명
    .well= survey.content
    h4 설문 조사 
    p
    | #{survey.selecttitle}
    p
    input.select(type='radio', id='select', name='select',value='1')
    | #{survey.select[0]}
    input.select(type='radio', id='select', name='select',value='2')
    | #{survey.select[1]}
    input.select(type='radio', id='select', name='select',value='3')
    | #{survey.select[2]}
    input.select(type='radio', id='select', name='select',value='4')
    | #{survey.select[3]}
    input.select(type='radio', id='select', name='select',value='5')
    | #{survey.select[4]}
    p
    | #{survey.texttitle}
    .well= survey.text  
    | #{survey.longtexttitle}
    .well= survey.longtext    
    
    hr
    h3 Comment (#{survey.numReply})
    table.table
      tr
        th email
        th content
        th time
      each reply in replys
        tr
          td= reply.email
          td= reply.content
          td= moment(reply.createdAt).format('YYYY/MM/DD')
    
    form.form-inline(action='/survey/' + survey._id + '/replys', method='POST')
      .form-group
        label.sr-only(for='email') email
        input.form-control(name='email', placeholder='email')
      .form-group
        label.sr-only(for='content') content
        input.form-control(name='content', size=50, placeholder='content')
        //- input.form-control(name='content', type='radio', value='1',checked=(val===1))
        //- label 1
        //- select(name='content')
          //- option(value='aaa') AAA
          //- input(type='checkbox', name='content', value='1')
            //- label 운동
      button.btn.btn-info 댓글저장
      
    hr
    a.btn.btn-default(href='/survey') 전체 목록 
    

    //- p
    //-   b by #{survey.number}
    //-   | 
    //-   span at #{moment(survey.createdAt).format('YYYY-MM-DD HH:mm:ss')} 
    //-   | 
    //-   | (조회수 #{survey.read})
    //- p.well(style='white-space:pre;')= survey.content
  
